meta {
  name: 17 - Hide campaign from third party
  type: http
  seq: 17
}

get {
  url: {{baseUrl}}/campaigns/{{campaignId}}
  body: none
  auth: none
}

tests {
  test('masque la campagne au tiers', function() {
    expect(res.getStatus()).to.equal(404);
    expect(res.getBody().message).to.equal('Campaign not found');
  });
}
