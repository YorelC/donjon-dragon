meta {
  name: 46 - Invite Legolas for membership
  type: http
  seq: 46
}

post {
  url: {{baseUrl}}/campaigns/{{campaignId}}/invitations
  body: json
  auth: none
}

headers {
  content-type: application/json
  x-csrf-token: {{csrfToken}}
  Idempotency-Key: {{finalLegolasInviteKey}}
}

body:json {
  { "displayName": "{{thirdDisplayName}}" }
}

tests {
  test('invite le second joueur du scénario de rôles', function() {
    expect(res.getStatus()).to.equal(204);
  });
}
