meta {
  name: 15 - Count Frodo invitations
  type: http
  seq: 15
}

get {
  url: {{baseUrl}}/campaigns/invitations/count
  body: none
  auth: none
}

tests {
  test('compte une invitation ouverte', function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().count).to.equal(1);
  });
}
