meta {
  name: 25 - Invite Legolas second cycle
  type: http
  seq: 25
}

post {
  url: {{baseUrl}}/campaigns/{{campaignId}}/invitations
  body: json
  auth: none
}

headers {
  content-type: application/json
  x-csrf-token: {{csrfToken}}
  Idempotency-Key: {{secondInviteKey}}
}

body:json {
  {
    "displayName": "{{thirdDisplayName}}"
  }
}

tests {
  test('crée le second cycle', function() {
    expect(res.getStatus()).to.equal(204);
  });
}
