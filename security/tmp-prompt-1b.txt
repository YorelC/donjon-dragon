Deja existant:
- shared/src/auth-schema.ts (LoginDto, RegisterDto, RefreshDto, TokenPayload)
- back/src/auth/domain/ (auth.repository.port.ts, auth.entity.ts, index.ts) - deja cree

1. Cree back/src/auth/infrastructure/auth-schemas.ts:
   - Mongoose schema User: { email (String, unique, required), username (String, required), passwordHash (String, required), tier ({type: String, enum: ['full','readonly'], default: 'full'}), tenantId (String, required), createdAt (Date, default: now) }
   - Mongoose schema RefreshToken: { token (String, unique, required), userId (String, required, indexed), expiresAt (Date, required, indexed, TTL), revoked (Boolean, default: false), createdAt (Date, default: now) }
   - Exporte UserModel (model('User', userSchema)) et RefreshTokenModel (model('RefreshToken', refreshTokenSchema))

2. Cree back/src/auth/infrastructure/auth-mongo.repository.ts:
   - Implements IAuthRepository (from domain/auth.repository.port.ts)
   - Injecte UserModel et RefreshTokenModel via @InjectModel decorators
   - create: new UserModel(user).save()
   - findByEmail: UserModel.findOne({ email }).exec()
   - findById: UserModel.findById(id).exec()
   - saveRefreshToken: new RefreshTokenModel({ token, userId, expiresAt }).save()
   - findRefreshToken: RefreshTokenModel.findOne({ token, revoked: false }).exec()
   - revokeRefreshToken: RefreshTokenModel.updateOne({ token }, { revoked: true }).exec()
   - revokeAllUserTokens: RefreshTokenModel.updateMany({ userId, revoked: false }, { revoked: true }).exec()

3. Cree back/src/auth/infrastructure/jwt.strategy.ts:
   - extends PassportStrategy(Strategy)
   - constructor: super({ jwtFromRequest: ExtractJwt.fromAuthHeaderAsBearerToken(), secretOrKey: process.env.JWT_SECRET || 'dev-secret-change-me' })
   - validate(payload: TokenPayload): Promise<{ userId, email, tier, tenantId }>
   - Si payload invalide → throw UnauthorizedException

4. Cree back/src/auth/infrastructure/index.ts

Contraintes: fonctions ≤ 20 lignes, fichiers ≤ 150 lignes, any interdit, kebab-case.
Apres: pnpm typecheck && pnpm test