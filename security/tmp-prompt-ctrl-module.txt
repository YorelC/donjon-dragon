Complete auth module: controller + module + app.module + tests.

APRES reshape: IAuthRepository, auth.entity.ts (createUser/validatePassword), auth.service.ts, auth-mongo.repository.ts, in-memory-auth.repository.ts, jwt.strategy.ts, decorators, guards sont deja en place.

A CREER:

1. Cree back/src/auth/interface/auth.controller.ts:
   - POST /auth/register: @Body(RegisterDto) body → authService.register(body)
   - POST /auth/login: @Body(LoginDto) body → authService.login(body)
   - POST /auth/refresh: @Body(RefreshDto) body → authService.refreshToken(body)
   - GET /auth/me: @UseGuards(JwtAuthGuard) @CurrentUser() user → authService.getProfile(user.sub)
   - @Public() sur register et login
   - Importe LoginDto/RegisterDto/RefreshDto depuis shared/src/auth-schema.ts

2. Cree back/src/auth/interface/auth.module.ts:
   - Imports: MongooseModule.forFeature([{ name: 'User', schema: userSchema }, { name: 'RefreshToken', schema: refreshTokenSchema }]), JwtModule.register({ secret: process.env.JWT_SECRET || 'dev-secret-change-me', signOptions: { expiresIn: '15m' } }), PassportModule
   - Imports: forwardRef(() => AppModule) si circular
   - Providers: AuthService, JwtStrategy, { provide: 'AUTH_REPOSITORY', useClass: AuthMongoRepository }
   - Controllers: [AuthController]
   - Exports: [AuthService]

3. Modifie back/src/app.module.ts:
   - Importe AuthModule (en haut du @Module decorator)
   - Ajoute APP_GUARD: { provide: APP_GUARD, useClass: JwtAuthGuard } dans providers
   - Garde les imports existants (CharacterModule, CombatModule)

4. Cree back/src/auth/interface/auth.controller.test.ts:
   - Test register: 201 avec user + tokens
   - Test login: 200 avec tokens
   - Test me: 401 sans token, 200 avec token valide
   - Mock AuthService

Execute: pnpm typecheck && cd back && pnpm test. Fais passer tous les tests verts.