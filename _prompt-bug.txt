bug frontend : page /profile/friends affiche 'Chargement...' meme quand la requete API a repondu (tableau vide).

causes possibles :
1. TanStack Query isLoading reste true si la requete API echoue (401, 500, CORS) -> verifier que l'appel API fonctionne
2. Le format de reponse API ne correspond pas au type attendu -> verifier ce que renvoie GET /api/friends
3. Le enabled prop empeche la requete de demarrer

diagnostic :
- curl http://localhost:3000/api/friends avec un vrai JWT -> quel status ? quel body ?
- verifier que la reponse est bien un tableau JSON (pas {data: [...]} ou autre wrapper)
- verifier que le queryKey ["friends", "list"] n'est pas invalide ailleurs

corrige le probleme pour que :
- si API repond avec [] -> affiche "Tu n'as pas encore d'amis."
- si API repond avec une erreur -> affiche l'erreur
- si API charge -> affiche "Chargement..."

typecheck + test OK apres correction.