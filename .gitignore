node_modules
back/dist/
front/dist/
.lh/
*.log
.env
.env.local

# Reglages Claude Code personnels : la liste de permissions et les hooks locaux
# n'ont pas a etre partages. `.claude/settings.json` et `.claude/rules/`, eux,
# sont versionnes a dessein.
.claude/settings.local.json

docs/books/*
docs/ui-design/*

# Cache pnpm local et bac a sable de travail : ni l'un ni l'autre n'a de valeur
# partagee, et tmp/ porte des images lourdes.
.pnpm-store/
tmp/